version: '3.8'

services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: devalliance-frontend
    ports:
      - "3000:80"
    restart: unless-stopped
    networks:
      - devalliance-network

networks:
  devalliance-network:
    external: true
    name: devalliance-network
